---
import { getCollection, render } from "astro:content";
import ContentLayout from "@layouts/ContentLayout.astro";

export async function getStaticPaths() {
  const projects = await getCollection("projects", ({ data }) => !data.draft);
  return projects.map((project) => ({
    params: { slug: project.id },
    props: { project }
  }));
}

const { project } = Astro.props;
const { Content } = await render(project);
---

<ContentLayout
  title={project.data.title}
  description={project.data.description}
  date={project.data.date}
  tags={project.data.tags}
>
  <div class="card">
    <p><strong>Repository:</strong> <a href={project.data.repo}>{project.data.repo}</a></p>
    {project.data.homepage && <p><strong>Homepage:</strong> <a href={project.data.homepage}>{project.data.homepage}</a></p>}
    <p><strong>Status:</strong> <span class={`status ${project.data.status}`}>{project.data.status}</span></p>
  </div>
  <Content />
</ContentLayout>
